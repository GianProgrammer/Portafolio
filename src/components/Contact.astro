---
import ContactForm from '../components/ContactForm.jsx';

const contactInfo = [
  {
    type: 'email',
    label: 'Gmail',
    value: 'Gianferreyra2014@gmail.com',
    url: 'mailto:Gianferreyra2014@gmail.com',
    icon: '📧',
    color: 'from-red-500 to-orange-400'
  },
  {
    type: 'github',
    label: 'GitHub',
    value: 'GianProgrammer',
    url: 'https://github.com/GianProgrammer',
    icon: '🐙',
    color: 'from-gray-700 to-gray-500'
  },
  {
    type: 'linkedin',
    label: 'LinkedIn',
    value: 'Gianluca Ferreyra',
    url: 'https://linkedin.com/in/gianluca-ferreyra',
    icon: '💼',
    color: 'from-blue-600 to-blue-400'
  },
  {
    type: 'phone',
    label: 'WhatsApp',
    value: '+5491123119631',
    url: 'https://wa.me/5491123119631',
    icon: '💬',
    color: 'from-green-500 to-emerald-400'
  }
];
---

<section id="contact" class="py-20 relative">
  <div class="container mx-auto px-6">
    <h2 class="text-4xl md:text-5xl font-bold font-space gradient-text text-center mb-12">
      Contacto
    </h2>

    <div class="max-w-4xl mx-auto">
      <div class="grid md:grid-cols-2 gap-8">
        <!-- Contact Info -->
        <div class="space-card p-8 rounded-2xl backdrop-blur-lg border border-neon-purple/30">
          <h3 class="text-2xl font-bold text-neon-cyan mb-6">📞 Conectemos</h3>
          <p class="text-gray-300 mb-6">
            ¿Tienes un proyecto en mente? ¡Me encantaría escuchar sobre tus ideas!
          </p>

          <div class="space-y-4">
            {contactInfo.map(contact => (
              <a href={contact.url} target="_blank" rel="noopener noreferrer"
                 class={`flex items-center gap-4 p-4 rounded-lg bg-gradient-to-r ${contact.color} hover-lift transition-all duration-300 group`}>
                <div class="text-3xl group-hover:scale-110 transition-transform duration-300">{contact.icon}</div>
                <div>
                  <div class="font-semibold text-white">{contact.label}</div>
                  <div class="text-white/80 text-sm">{contact.value}</div>
                </div>
              </a>
            ))}
          </div>
        </div>

        <!-- Contact Form -->
        <div class="space-card p-8 rounded-2xl backdrop-blur-lg border border-neon-cyan/30">
          <h3 class="text-2xl font-bold text-neon-purple mb-6">✉️ Envíame un mensaje</h3>

          <form id="contact-form" class="space-y-6" action="#">
            <div>
              <label for="name" class="block text-neon-cyan font-semibold mb-2">Nombre</label>
              <input type="text" id="name" name="name" placeholder="Tu nombre" required
                     class="w-full px-4 py-3 bg-space-dark/50 border border-neon-cyan/30 rounded-lg text-white placeholder-gray-400 focus:border-neon-cyan focus:outline-none focus:ring-2 focus:ring-neon-cyan/20 transition-all duration-300"/>
            </div>

            <div>
              <label for="email" class="block text-neon-cyan font-semibold mb-2">Email</label>
              <input type="email" id="email" name="email" placeholder="tu@email.com" required
                     class="w-full px-4 py-3 bg-space-dark/50 border border-neon-cyan/30 rounded-lg text-white placeholder-gray-400 focus:border-neon-cyan focus:outline-none focus:ring-2 focus:ring-neon-cyan/20 transition-all duration-300"/>
            </div>

            <div>
              <label for="subject" class="block text-neon-cyan font-semibold mb-2">Asunto</label>
              <input type="text" id="subject" name="subject" placeholder="¿De qué se trata?" required
                     class="w-full px-4 py-3 bg-space-dark/50 border border-neon-cyan/30 rounded-lg text-white placeholder-gray-400 focus:border-neon-cyan focus:outline-none focus:ring-2 focus:ring-neon-cyan/20 transition-all duration-300"/>
            </div>

            <div>
              <label for="message" class="block text-neon-cyan font-semibold mb-2">Mensaje</label>
              <textarea id="message" name="message" rows={5} placeholder="Cuéntame sobre tu proyecto o consulta..." required
                        class="w-full px-4 py-3 bg-space-dark/50 border border-neon-cyan/30 rounded-lg text-white placeholder-gray-400 focus:border-neon-cyan focus:outline-none focus:ring-2 focus:ring-neon-cyan/20 transition-all duration-300 resize-none"/>
            </div>

            <button type="submit"
                    class="w-full py-3 bg-gradient-to-r from-neon-cyan to-neon-purple text-black font-bold rounded-lg hover-lift transition-all duration-300 shadow-lg shadow-neon-cyan/50">
              🚀 Enviar Mensaje
            </button>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Componente que carga EmailJS solo en el navegador -->
  <ContactForm client:load />
</section>
