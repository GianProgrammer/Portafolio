---
import Layout from '../../layouts/Layout.astro';

const project = {
  title: "Sistema de Turnos - NutriStetic",
  description: "Sistema completo de gestión de turnos con autenticación, panel administrativo y calendario interactivo",
  technologies: ["Node.js", "Express", "React", "MongoDB", "JWT", "Bootstrap"],
  features: [
    "Autenticación de usuarios con JWT",
    "Panel administrativo completo",
    "Calendario interactivo para gestión de turnos",
    "Sistema de notificaciones en tiempo real",
    "Diferentes roles de usuario (admin, empleado, cliente)",
    "Interfaz responsiva y moderna",
    "Base de datos NoSQL con MongoDB"
  ],
  githubUrl: "https://github.com/GianProgrammer/NutriStetic-Turnos",
  demoUrl: "#", // TODO: Agregar URL de demo cuando esté disponible
  image: "/images/sistema-turnos-preview.png" // TODO: Reemplazar con captura real del proyecto
};
---

<Layout 
  title={project.title}
  description={project.description}
  keywords="sistema turnos, node.js, react, mongodb, gestión turnos, calendario"
>
  <section class="py-20 min-h-screen">
    <div class="container mx-auto px-6">
      <!-- Back Button -->
      <a 
        href="/"
        class="inline-flex items-center gap-2 text-neon-cyan hover:text-neon-purple transition-colors duration-300 mb-8"
      >
        <span>←</span>
        Volver al inicio
      </a>
      
      <!-- Project Header -->
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h1 class="text-4xl md:text-5xl font-bold font-space gradient-text mb-4">
            {project.title}
          </h1>
          <p class="text-xl text-gray-300 max-w-3xl mx-auto">
            {project.description}
          </p>
        </div>
        
        <!-- Project Image Placeholder -->
        <div class="mb-12">
          <div class="bg-gradient-to-br from-neon-cyan/20 to-neon-purple/20 rounded-2xl p-8 text-center border border-neon-cyan/30">
            <div class="text-6xl mb-4">📅</div>
            <h3 class="text-2xl font-bold text-neon-cyan mb-2">Vista previa del proyecto</h3>
            <p class="text-gray-400">
              {/* TODO: Reemplazar con captura real del proyecto */}
              Aquí irá una captura del Sistema de Turnos en funcionamiento
            </p>
          </div>
        </div>
        
        <!-- Technologies -->
        <div class="mb-12">
          <h2 class="text-3xl font-bold text-neon-purple mb-6">🛠️ Tecnologías Utilizadas</h2>
          <div class="flex flex-wrap gap-3">
            {project.technologies.map((tech) => (
              <span class="px-4 py-2 bg-gradient-to-r from-neon-cyan to-neon-purple text-black font-semibold rounded-full">
                {tech}
              </span>
            ))}
          </div>
        </div>
        
        <!-- Features -->
        <div class="mb-12">
          <h2 class="text-3xl font-bold text-neon-cyan mb-6">✨ Características</h2>
          <div class="grid md:grid-cols-2 gap-4">
            {project.features.map((feature) => (
              <div class="flex items-start gap-3 p-4 bg-space-dark/50 rounded-lg border border-neon-purple/30">
                <span class="text-neon-cyan text-xl">•</span>
                <span class="text-gray-300">{feature}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Links -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href={project.githubUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center gap-2 px-8 py-3 bg-gradient-to-r from-gray-700 to-gray-900 text-white font-semibold rounded-lg hover-lift transition-all duration-300 border border-gray-600"
          >
            <span>📁</span>
            Ver en GitHub
          </a>
          
          <a 
            href={project.demoUrl}
            class="inline-flex items-center justify-center gap-2 px-8 py-3 bg-gradient-to-r from-neon-cyan to-neon-purple text-black font-semibold rounded-lg hover-lift transition-all duration-300 shadow-lg shadow-neon-cyan/50"
          >
            <span>🚀</span>
            Ver Demo
          </a>
        </div>
        
        <!-- Note -->
        <div class="mt-12 p-6 bg-yellow-500/10 border border-yellow-500/30 rounded-lg">
          <div class="flex items-start gap-3">
            <span class="text-yellow-500 text-xl">⚠️</span>
            <div>
              <h3 class="text-yellow-500 font-semibold mb-2">Nota Importante</h3>
              <p class="text-gray-300 text-sm">
                Este proyecto fue desarrollado como parte de mi formación académica. 
                {/* TODO: Actualizar con información real del proyecto */}
                La demo y capturas de pantalla estarán disponibles próximamente.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  @import '../../styles/global.css';
</style>