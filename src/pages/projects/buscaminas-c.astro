---
import Layout from '../../layouts/Layout.astro';

const project = {
  title: "Buscaminas en C",
  description: "Implementación del clásico juego Buscaminas en lenguaje C con interfaz de línea de comandos",
  technologies: ["C", "ANSI C", "Algoritmos", "Estructuras de Datos"],
  features: [
    "Interfaz de línea de comandos interactiva",
    "Generación aleatoria de minas",
    "Sistema de marcado de casillas",
    "Detección de victoria/derrota",
    "Diferentes niveles de dificultad",
    "Código limpio y documentado",
    "Manejo eficiente de memoria"
  ],
  githubUrl: "https://github.com/GianProgrammer/MineSweeper-C",
  image: "/images/buscaminas-preview.png" // TODO: Reemplazar con captura real del proyecto
};
---

<Layout 
  title={project.title}
  description={project.description}
  keywords="buscaminas, c programming, cli, juego, algoritmos"
>
  <section class="py-20 min-h-screen">
    <div class="container mx-auto px-6">
      <!-- Back Button -->
      <a 
        href="/"
        class="inline-flex items-center gap-2 text-neon-cyan hover:text-neon-purple transition-colors duration-300 mb-8"
      >
        <span>←</span>
        Volver al inicio
      </a>
      
      <!-- Project Header -->
      <div class="max-w-4xl mx-auto">
        <div class="text-center mb-12">
          <h1 class="text-4xl md:text-5xl font-bold font-space gradient-text mb-4">
            {project.title}
          </h1>
          <p class="text-xl text-gray-300 max-w-3xl mx-auto">
            {project.description}
          </p>
        </div>
        
        <!-- Project Image Placeholder -->
        <div class="mb-12">
          <div class="bg-gradient-to-br from-neon-purple/20 to-neon-cyan/20 rounded-2xl p-8 text-center border border-neon-purple/30">
            <div class="text-6xl mb-4">💣</div>
            <h3 class="text-2xl font-bold text-neon-purple mb-2">Vista previa del juego</h3>
            <p class="text-gray-400">
              {/* TODO: Reemplazar con captura real del proyecto */}
              Aquí irá una captura del Buscaminas en funcionamiento en la terminal
            </p>
          </div>
        </div>
        
        <!-- Technologies -->
        <div class="mb-12">
          <h2 class="text-3xl font-bold text-neon-cyan mb-6">🛠️ Tecnologías Utilizadas</h2>
          <div class="flex flex-wrap gap-3">
            {project.technologies.map((tech) => (
              <span class="px-4 py-2 bg-gradient-to-r from-blue-500 to-cyan-400 text-white font-semibold rounded-full">
                {tech}
              </span>
            ))}
          </div>
        </div>
        
        <!-- Features -->
        <div class="mb-12">
          <h2 class="text-3xl font-bold text-neon-purple mb-6">🎯 Características</h2>
          <div class="grid md:grid-cols-2 gap-4">
            {project.features.map((feature) => (
              <div class="flex items-start gap-3 p-4 bg-space-dark/50 rounded-lg border border-neon-cyan/30">
                <span class="text-neon-purple text-xl">•</span>
                <span class="text-gray-300">{feature}</span>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Code Snippet -->
        <div class="mb-12">
          <h2 class="text-3xl font-bold text-neon-cyan mb-6">💻 Fragmento de Código</h2>
          <div class="bg-space-dark/80 rounded-lg p-6 border border-gray-700 overflow-x-auto">
            <pre class="text-green-400 text-sm"><code>{`// Estructura principal del juego
typedef struct
{
    bool tiene_mina;
    bool abierta;
    bool bandera;
    bool pregunta;
    int minas_alrededor;

}tCelda;


// Funcion para inicializar tablero
void** matrizCrear(size_t filas, size_t columnas, size_t tamElem)
{
    void** m = (void**)malloc(sizeof(void*)*filas);
    if(!m)
        return NULL;

    void** ult = m + filas - 1;

    for(void** i=m; i<=ult ; i++)
    {
        *i = (void*)malloc(tamElem*columnas);
        if(!*i)
        {
            destruirMatriz(m, i-m);
            return NULL;
        }
    }

    return m;
}
}`}</code></pre>
          </div>
        </div>
        
        <!-- Links -->
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <a 
            href={project.githubUrl}
            target="_blank"
            rel="noopener noreferrer"
            class="inline-flex items-center justify-center gap-2 px-8 py-3 bg-gradient-to-r from-gray-700 to-gray-900 text-white font-semibold rounded-lg hover-lift transition-all duration-300 border border-gray-600"
          >
            <span>📁</span>
            Ver en GitHub
          </a>
        </div>
        
        <!-- Learning Note -->
        <div class="mt-12 p-6 bg-blue-500/10 border border-blue-500/30 rounded-lg">
          <div class="flex items-start gap-3">
            <span class="text-blue-500 text-xl">🎓</span>
            <div>
              <h3 class="text-blue-500 font-semibold mb-2">Aprendizaje Obtenido</h3>
              <p class="text-gray-300 text-sm">
                Este proyecto me permitió profundizar en el manejo de memoria dinámica en C, 
                estructuras de datos complejas y algoritmos de búsqueda. Además, mejoré mis 
                habilidades en debugging y optimización de código.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  @import '../../styles/global.css';
</style>